# crossref-preprints-harvester
Collects **Crossref `posted-content`** (preprints and related) into a single wide table with robust guards: adaptive window splitting, hardened cursor loops, and a DOI-first fallback for dense windows.
